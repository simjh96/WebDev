package lecture;

import java.util.Vector;

class Point {
	private int x,y;

	public Point(int x, int y) {
		this.x = x;
		this.y = y;
	}

	@Override
	public String toString() {
		return "Point [x=" + x + ", y=" + y + "]";
	}
	
	public boolean test() {
		return true;
	}
}

public class PointVectorTest {
	public static void main(String[] args) {
		Vector<Object> pointVector = new Vector<>();
		pointVector.add(new Point(3,3));
		pointVector.add(new Point(10,10));
		pointVector.add(new Point(100,100));
		System.out.println(pointVector);
		for (int i = 0; i < pointVector.size(); i++) {
			System.out.println(((Point)pointVector.get(i)).test());
		}
	}

}
